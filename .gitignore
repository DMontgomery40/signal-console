node_modules/
dist/
build/
.turbo/
.next/
coverage/
*.log
.DS_Store
.env
.env.local
.env.*.local

# Local ralph runtime state
scripts/ralph/.last-branch
scripts/ralph/.last-story
scripts/ralph/.story-attempts
scripts/ralph/ralph.log
scripts/ralph/.playwright-mcp/

# Playwright MCP browser-snapshot residue (page-*.yml dumps); regenerable
.playwright-mcp/

# Gold DB (54 GB) — never tracked
data/*.sqlite
data/*.sqlite-wal
data/*.sqlite-shm
data/*.sqlite.pre-*
data/*.sqlite.corrupt-*

# Worker runtime heartbeat (updated each ingest cycle)
apps/worker/data/heartbeat.json

# Editor and assistant local state
.agents/
.claude/scheduled_tasks.lock

# Generated GitNexus / Agent Memory index; local-only and can be very large
.agent-memory-nexus/project-index/
.agent-memory-nexus/*.log

# Python sidecar virtualenv + pytest cache
.venv/
.pytest_cache/
__pycache__/
*.pyc

# NBA Quant Lab: generated snapshots/runs/pulls (parquet/duckdb/json artifacts)
# and the bootstrap-generated starter notebook — regenerable via `pnpm quant:*`.
outputs/nba-quant-lab/
# Backfill driver logs/scripts (generated per-run; stray *.sh here is not source).
outputs/backfill-nba-history/
notebooks/
