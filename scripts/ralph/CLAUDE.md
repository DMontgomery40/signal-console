# Ralph Runtime Notes — Retired

The original PRD-driven Ralph loop is retired. This file is intentionally no longer an autonomous agent contract.

Current Signal Console authority:

- closest repo `AGENTS.md` and `CLAUDE.md`
- `.codex/memory.md`
- `~/.codex/projects/-Users-davidmontgomery-signal-console/MEMORY.md`
- active `docs/*.md` files that are not marked historical
- current source code and tests

Do not resume the old one-story-per-iteration Ralph loop from this directory. Do not use the deleted root `PRD.md`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`, `scripts/ralph/ralph.log`, watchdog logs, or Playwright captures as current product authority.

Historical artifacts in this directory may still be useful for archaeology, but every claim from them must be checked against current repo docs, code, tests, and live data before use.
