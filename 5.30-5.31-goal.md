
<mainPrompt> Before answering the Claude choice, read the project’s Claude-side history too.

Required memory/context pass:
- Read the closest repo AGENTS.md.
- Read .codex/memory.md.
- Read repo-local Codex project memory under ~/.codex/projects/-Users-davidmontgomery-signal-console/.
- Read Claude project memory/transcripts under ~/.claude/projects/-Users-davidmontgomery-signal-console/.
- Search Claude history for: board-anomaly, board volatility, incident, fanout, play-by-play, suspend, F-010, F-012, baseline, game-state volatility, Signal Console, nba-predict.
- Do not rely on the audit finding alone. The audit proves current reachability, not product intent or historical value.
- Do not classify F-010 until the Claude memories and repo-local memories have been read and reconciled with the actual code.

You are in /Users/davidmontgomery/signal-console.

First, read the closest AGENTS.md and follow it. Also read .codex/memory.md before making any architectural/product judgment. Use repo-local memory only, per the project instructions.

Situation:
The user is in the middle of a Claude prompt-choice flow about audit fixes. A previous assistant badly overreached and recommended deleting or classifying F-010 without understanding the product. Do not repeat that mistake.

You are in /Users/davidmontgomery/signal-console.

First, read the closest AGENTS.md and follow it. Also read .codex/memory.md before making any architectural/product judgment. Use repo-local memory only, per the project instructions.

Situation:
The user is in the middle of a Claude prompt-choice flow about audit fixes. A previous assistant badly overreached and recommended deleting or classifying F-010 without understanding the product. Do not repeat that mistake.

The product is not “just typecheck cleanup.” Signal Console is a trader/operator/research product for detecting sports-market incidents: board-wide shocks, source divergence, stale books, prop/team-player fanout, play-by-play context, and timing around market reaction. The board-volatility signal is the tripwire, but the incident/fanout/replay layer may be core product work. Treat any board-anomaly code as potentially valuable until proven otherwise.

Immediate Claude prompt:
F-010 asks whether the board-anomaly subsystem is staged or abandoned:
1. Abandoned — remove it
2. Staged — keep & gate
3. Decide later

Answer: choose **3. Decide later** or type a custom answer saying:

“Do not delete or classify F-010 in this session. Apply only the gate fix from the first question. For F-010, produce a factual inventory first: every board-anomaly file, what behavior it encodes, current live/non-live callers, relation to the live board-volatility / incident / fanout product path, and exactly what would be lost by deleting it. No removal, no staged/abandoned label, no F-009 alias cleanup, and no baseline-action change until that inventory is reviewed by the owner.”

Important facts already established:
- The first gate-fix question should be answered with option 1: set both exactOptionalPropertyTypes: false and noUncheckedIndexedAccess: false in scripts/tsconfig.json, matching the deliberate package policy in packages/shared, packages/adapters, and apps/worker.
- The F-012 barrel import was the trigger, but the root issue is the scripts typecheck compiling transitive package internals under stricter flags than the packages themselves use.
- F-010 says the board-anomaly subsystem is currently unreachable from live routes/scripts and that board-volatility-baseline-rebuild writes board_volatility_baselines, which nothing reads.
- “0 current live callers” is not the same as “worthless.” It may be orphaned, superseded, staged, or a valuable research/spec prototype for the incident layer.
- Do not delete anything in F-010 to remove type errors.
- Do not call the subsystem abandoned.
- Do not call it staged as a final classification.
- Inventory first, owner review second, action third.

Files/areas to inspect for inventory:
- audit/findings/F-010-board-anomaly-cluster-unwired.md
- audit/findings/F-012-oddsapi-key-gate-omits-preferred-name.md
- audit/HANDOFF-audit-fixes-2026-05-30.md
- packages/shared/src/board-anomaly/**
- packages/shared/src/board-anomaly-*.ts
- packages/shared/src/board-volatility-baselines.ts
- packages/shared/src/index.ts
- packages/domain/src/board-anomaly.ts
- apps/worker/src/index.ts
- current live board/fanout/API paths under apps/api/src/services and apps/api/src/routes

Deliverable:
Give the user a sober, evidence-based inventory and a recommendation only after understanding what each piece does. If mutating later, follow AGENTS: run GitNexus impact analysis before editing symbols, add behavior-level tests for defects, run changed-surface tests and repo verify, and do not revert unrelated dirty worktree changes.</mainPrompt>

<knowledgePathToAnswerTheRealQuestion> Thorough Code Review and Frontend Code Review </knowledgePathToAnswerTheRealQuestion>
<gitHell> you must navigate every weirdly hidden claude worktree. every branch. and you must masterfully gather the threads, organize them, cut them, tie them toether, realize those two shouldn't touch, gently clip them together for now and then -- youmust have the full design, the full graph in your head, and abiliity so see so far around all corners by the end, you know exactly which branches did goog and are done, who's work needs to just be set on fire, everything </gitHell> start long running task , see the "loop" plugin or skill or whatever claude uses to accompllish "/loop". neither claude no codex has a good alternative to 'goal' with only 4000 characters, so use claudes' loop to adapt to your own harness, to complete this long runninug task

