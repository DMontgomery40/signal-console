{
  "name": "@signal-console/domain",
  "version": "0.0.0",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --out-dir dist",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "verify": "vitest run"
  },
  "dependencies": {
    "@signal-console/detectors": "workspace:*",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "vitest": "^3.2.0"
  }
}
