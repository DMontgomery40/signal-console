"""Research experiments: one-off, pre-registered falsification gates.

These are NOT part of the production model/eval pipeline. Each experiment is a
cheap, read-only kill-or-confirm test that must pass before a model build is
justified. See confluence_killtest for the whole-board confluence gate.
"""
